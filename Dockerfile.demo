FROM python:3.12-slim

WORKDIR /demo
COPY requirements-demo.txt .
RUN pip install --no-cache-dir -r requirements-demo.txt \
    && python -m playwright install --with-deps chromium

COPY demo ./demo
CMD ["python", "demo/record_demo.py"]
